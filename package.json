{
  "name": "mote",
  "private": true,
  "version": "2.0.0-alpha.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/copy-static.mjs",
    "preview": "vite preview",
    "check": "node --check src/app.js && node --check src/db.js && node --check src/format.js && node --check src/markdown.js && node --check scripts/copy-static.mjs && node --check sw.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "dompurify": "^3.2.6",
    "highlight.js": "^11.11.1",
    "marked": "^15.0.12",
    "mermaid": "^11.6.0"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  },
  "engines": {
    "node": ">=20"
  }
}
